# 부부 공동 가계부 설계 문서

## 1. 프로젝트 개요

부부가 같은 가계부를 여러 기기에서 함께 사용할 수 있는 웹 가계부다.  
GitHub Pages로 프론트엔드를 배포하고, Supabase를 백엔드 저장소로 사용한다.

핵심 목표는 다음과 같다.

- 데스크톱과 스마트폰에서 같은 데이터 확인
- 로그인 없이 간단한 코드 + PIN 방식으로 접속
- 수입 / 지출 / 고정비를 한곳에서 관리
- 월별 흐름과 일별 흐름을 시각적으로 확인
- 입력 탭 안에서 바로 내역 확인 및 삭제 가능

---

## 2. 접속 방식

### 2.1 기본 방식

사용자는 첫 화면에서 아래 두 정보를 입력해 가계부에 입장한다.

- 가계부 코드
- PIN

이 두 값을 조합해 브라우저에서 `SHA-256` 기반 `household_key`를 만들고, 같은 코드와 PIN을 입력한 기기끼리 같은 데이터에 접근한다.

### 2.2 특징

- 별도 회원가입 / 로그인 없음
- 같은 코드 + PIN이면 데스크톱과 모바일이 연동됨
- 세션 정보는 브라우저 `localStorage`에 저장됨

---

## 3. 전체 화면 구조

현재 가계부는 탭 기반 화면으로 구성한다.

1. 일별 통계
2. 월별 통계
3. 수입 입력
4. 지출 입력
5. 고정비 관리
6. 전체 내역

상단에는 현재 연결된 가계부 코드와 주요 액션 버튼이 있다.

- 새로고침
- 가계부 변경

---

## 4. 탭별 설계

## 4.1 일별 통계

### 목적

이번 달의 일별 수입 / 지출 흐름을 빠르게 파악한다.

### 구성

- 이번 달 요약 카드
- 오늘 수입
- 오늘 지출
- 오늘 고정비
- 오늘 잔액
- 일별 수입 / 지출 그래프

### 그래프 요구사항

- 수입과 지출을 일자별로 막대 형태로 표시
- 그래프 왼쪽에 금액 기준 Y축 표시
- Y축 표기는 원화 기준
- 금액 표기는 쉼표 포함
- 예: `300,000원`, `200,000원`, `100,000원`, `0원`

### 포함 데이터

- 일반 수입 내역
- 일반 지출 내역
- 자동 반영 설정된 고정비의 해당 날짜 반영분

---

## 4.2 월별 통계

### 목적

최근 몇 개월간 수입 / 지출 / 잔액 흐름을 확인한다.

### 구성

- 고정비 합계
- 변동 지출
- 월 평균 지출
- 총 지출
- 총 수입
- 예상 잔액
- 월별 그래프

### 포함 데이터

- 일반 수입 합계
- 일반 지출 합계
- 자동 반영 대상 고정비 합계

---

## 4.3 수입 입력

### 목적

수입을 등록하고, 같은 탭 안에서 바로 수입 내역을 확인한다.

### 입력 항목

- 날짜
- 수입 구분
- 항목명
- 금액
- 입력한 사용자
- 메모

### 입력한 사용자

자유 입력이 아니라 드롭다운 선택 방식이다.

- 남편
- 아내

### 조회 영역

수입 입력 폼 아래에 수입 목록 표를 둔다.

### 수입 목록 표시 항목

- 날짜
- 항목명
- 입력한 사용자
- 메모
- 금액
- 삭제 버튼

### 기대 동작

- 수입 저장 후 새로고침 없이 목록이 즉시 갱신
- 전체 내역 탭으로 이동하지 않아도 수입 내역 확인 가능

---

## 4.4 지출 입력

### 목적

지출을 등록하고, 같은 탭 안에서 바로 지출 내역을 확인한다.

### 입력 항목

- 날짜
- 카테고리
- 항목명
- 금액
- 입력한 사용자
- 메모

### 입력한 사용자

드롭다운 선택 방식이다.

- 남편
- 아내

### 조회 영역

지출 입력 폼 아래에 지출 목록 표를 둔다.

### 지출 목록 표시 항목

- 날짜
- 카테고리
- 항목명
- 입력한 사용자
- 메모
- 금액
- 삭제 버튼

### 기대 동작

- 지출 저장 후 새로고침 없이 목록이 즉시 갱신
- 전체 내역 탭으로 이동하지 않아도 지출 내역 확인 가능

---

## 4.5 고정비 관리

### 목적

매달 반복되거나 참고용으로 관리할 비용을 별도로 저장한다.

### 입력 항목

- 일자
- 항목명
- 금액
- 자동 반영 여부
- 메모

### 자동 반영 여부

체크박스로 관리한다.

- 체크됨: 매월 소비에 자동 반영
- 체크 해제: 목록에만 저장하고 자동 반영 안 함

### 자동 반영함 선택 시

- 월별 소비 내역에 자동 포함
- 월별 지출 합계에 포함
- 월별 통계 그래프에 포함
- 일별 통계에서도 해당 날짜에 반영
- 전체 내역 탭에도 고정비 발생 내역으로 표시

### 자동 반영하지 않음 선택 시

- 고정비 목록에는 저장됨
- 월별 소비 합계에 자동 포함되지 않음
- 월별 그래프에 반영되지 않음
- 일별 통계에도 반영되지 않음
- 전체 내역 자동 발생 목록에도 포함되지 않음

### 고정비 목록 표시 항목

- 항목명
- 기준 일자
- 메모
- 자동 반영 상태 배지
- 금액
- 삭제 버튼

---

## 4.6 전체 내역

### 목적

수입, 지출, 자동 반영된 고정비 발생 내역을 한 번에 조회한다.

### 필터

- 구분
  - 전체
  - 수입
  - 지출
  - 고정비
- 카테고리
  - 전체
  - 현재 저장된 내역 기준 카테고리 목록

### 전체 내역 표시 항목

- 날짜
- 구분
- 카테고리
- 항목명
- 입력한 사용자
- 메모
- 금액
- 삭제 버튼

### 동작 방식

- 일반 거래 내역 + 자동 반영된 고정비 발생 내역을 합쳐서 보여줌
- 날짜 역순 정렬
- 필터 조건에 맞는 결과만 표시

---

## 5. 데이터 저장 구조

## 5.1 고정비 테이블

`public.shared_fixed_expenses`

주요 컬럼:

- `id`
- `household_key`
- `date`
- `name`
- `amount`
- `note`
- `auto_apply`
- `created_at`

의미:

- `date`: 고정비 기준 일자
- `name`: 고정비 항목명
- `auto_apply`: 월별 소비 자동 반영 여부

## 5.2 거래 테이블

`public.shared_transactions`

주요 컬럼:

- `id`
- `household_key`
- `date`
- `type`
- `category`
- `name`
- `amount`
- `created_by`
- `note`
- `created_at`

의미:

- `type`: `income` 또는 `expense`
- `category`: 수입/지출 분류
- `name`: 항목명
- `created_by`: 입력한 사용자

---

## 6. Supabase 함수 구조

주요 RPC 함수:

- `get_shared_budget(text)`
- `add_shared_fixed_expense(text, date, text, numeric, text, boolean)`
- `add_shared_transaction(text, date, text, text, text, numeric, text, text)`
- `delete_shared_fixed_expense(text, uuid)`
- `delete_shared_transaction(text, uuid)`

### 역할

- `get_shared_budget`: 현재 가계부의 고정비 / 거래 내역 전체 조회
- `add_shared_fixed_expense`: 고정비 추가
- `add_shared_transaction`: 수입 / 지출 추가
- 삭제 함수들: 각 항목 삭제

---

## 7. 고정비 자동 반영 로직

자동 반영 대상인 고정비는 다음 조건을 만족할 때 월별 / 일별 계산에 포함한다.

- `auto_apply = true`
- 고정비 기준 월이 조회 대상 월보다 미래가 아님

예를 들어,

- 기준 일자: `2026-05-10`
- 자동 반영: 체크됨

이면,

- 2026년 5월부터 현재 월까지 매월 10일에 고정비 발생 내역 생성
- 해당 월 지출 합계와 월별 그래프에 포함

반대로 자동 반영이 꺼져 있으면 고정비 목록에만 남고 통계에는 들어가지 않는다.

---

## 8. 동기화 방식

### 현재 방식

- Supabase RPC 호출로 데이터 저장 / 조회
- 접속 후 주기적으로 데이터 새로고침
- 수동 새로고침 버튼 제공

### 기대 효과

- 데스크톱에서 입력한 내역을 스마트폰에서 확인 가능
- 스마트폰에서 입력한 내역을 데스크톱에서도 확인 가능

---

## 9. 삭제 기능

현재 삭제 가능한 대상:

- 수입 내역
- 지출 내역
- 고정비 항목

삭제는 각 탭 안 목록 또는 전체 내역에서 버튼으로 수행한다.

---

## 10. 운영 시 주의사항

### 10.1 프론트 변경 후

정적 파일 변경만 있는 경우:

- GitHub에 푸시
- GitHub Pages 반영 후 새로고침

### 10.2 DB 구조 변경 후

`supabase-schema.sql`이 바뀐 경우에는 반드시 Supabase SQL Editor에서 다시 실행해야 한다.

대표 사례:

- 새 컬럼 추가
- RPC 함수 시그니처 변경
- 자동 반영 여부 필드 추가
- 항목명 / 입력한 사용자 필드 추가

---

## 11. 현재 구현 기준 핵심 기능 요약

- 코드 + PIN 기반 공유 가계부 접속
- 일별 통계
- 월별 통계
- 수입 입력 + 수입 내역 조회
- 지출 입력 + 지출 내역 조회
- 고정비 저장 + 자동 반영 여부 설정
- 전체 내역 필터 조회
- 입력 내역 삭제
- 여러 기기 동기화

---

## 12. 추후 확장 아이디어

- 사용자별 필터링
- 월 선택 드롭다운
- 카테고리별 통계 차트
- 고정비 수정 기능
- CSV 다운로드
- 백업 / 복원 기능
- 모바일 전용 레이아웃 개선
